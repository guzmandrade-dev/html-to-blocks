{
  "name": "html-to-blocks",
  "version": "1.0.0",
  "description": "Simple HTML scraper to get HTML from remote pages.",
  "main": "getRemoteHTML.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "GPL-3.0",
  "dependencies": {
    "@playwright/test": "*"
  }
}
